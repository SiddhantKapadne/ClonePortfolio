/** Canonical site origin (www). Used for meta tags, JSON-LD, and sitemap. */
export const SITE_CANONICAL_ORIGIN = 'https://www.siddhant.website'

/** Default Open Graph / Twitter image (served from /public). */
export const SITE_OG_IMAGE_PATH = '/og-image.png'
